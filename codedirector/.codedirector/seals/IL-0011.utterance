Option 2 — create docs/ROADMAP.md in the codedirector repo with a "0.4.6 candidates" section: Feature 8 (no-new-dependency can't tell a version bump from a dependency) and the new fingerprint gap (checkpoint must expand untracked directories into their files — false-positive scope violation on pre-existing untracked folders). I'll mirror them as GitHub issues under Yasirdora myself later. Under a lock, as always.